# ============================================================
# Global vars — semua node
# ============================================================

ansible_python_interpreter: /usr/bin/python3
ansible_ssh_common_args: "-o StrictHostKeyChecking=no"

# Tailscale
tailscale_version: stable
tailscale_args: "--accept-routes"
