---
# Ingress + TLS — ingress-nginx + cert-manager
# Requires: K3s harus sudah running

- name: Ingress NGINX Controller
  hosts: k3s_server
  roles:
    - ingress_nginx

- name: Cert-Manager (Let's Encrypt)
  hosts: k3s_server
  roles:
    - cert_manager
