apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: infra
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: local-path
  resources:
    requests:
      storage: ${GRAFANA_DATA_SIZE}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: infra
data:
  datasources.yaml: |
    apiVersion: 1
    datasources:
      - name: VictoriaMetrics
        type: prometheus
        uid: victoriametrics
        url: http://victoriametrics.infra.svc:8428
        access: proxy
        isDefault: true

      - name: Loki
        type: loki
        uid: loki
        url: http://loki.infra.svc:3100
        access: proxy
        jsonData:
          derivedFields:
            - name: traceID
              matcherRegex: '(?:trace_id|traceId|traceID|traceid)["\s:=]+["\s]*(\w{32,})'
              url: '$${__value.raw}'
              datasourceUid: tempo
            - name: traceID_short
              matcherRegex: '(?:trace_id|traceId|traceID|traceid)["\s:=]+["\s]*(\w{16,})'
              url: '$${__value.raw}'
              datasourceUid: tempo
            - name: profileId
              matcherRegex: '(?:profile_id|profileId)["\s:=]+["\s]*(\w+)'
              url: '$${__value.raw}'
              datasourceUid: pyroscope

      - name: Tempo
        type: tempo
        uid: tempo
        url: http://tempo.infra.svc:3200
        access: proxy
        jsonData:
          tracesToLogsV2:
            datasourceUid: loki
            spanStartTimeShift: "-5m"
            spanEndTimeShift: "5m"
            tags:
              - key: service.name
                value: service_name
              - key: service.name
                value: job
          tracesToMetrics:
            datasourceUid: victoriametrics
            queries:
              - name: "Request Rate"
                query: 'sum(rate(http_requests_total{service_name="$${__span.tags["service.name"]}"}[5m]))'
          tracesToProfiles:
            datasourceUid: pyroscope
            tags:
              - key: service.name
                value: service_name

      - name: Pyroscope
        type: grafana-pyroscope-datasource
        uid: pyroscope
        url: http://pyroscope.infra.svc:4040
        access: proxy
---
apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: infra
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    app: grafana
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
  namespace: infra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana
  template:
    metadata:
      labels:
        app: grafana
    spec:
      containers:
        - name: grafana
          image: grafana/grafana:latest
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: infra-secrets
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: datasources
              mountPath: /etc/grafana/provisioning/datasources
            - name: data
              mountPath: /var/lib/grafana
          resources:
            requests:
              memory: "${K8S_REQUESTS_MEMORY}"
              cpu: "${K8S_REQUESTS_CPU}"
            limits:
              memory: "${K8S_LIMITS_MEMORY}"
              cpu: "${K8S_LIMITS_CPU}"
      volumes:
        - name: datasources
          configMap:
            name: grafana-datasources
        - name: data
          persistentVolumeClaim:
            claimName: grafana-data
