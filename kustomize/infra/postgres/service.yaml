apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: infra
  annotations:
    tailscale.com/hostname: postgres-faisalaffan
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres
